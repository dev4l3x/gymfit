<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:effects="clr-namespace:FitnessTrack.Effects"
             mc:Ignorable="d"
             x:Class="FitnessTrack.CustomControls.RoundedEntry"
             BackgroundColor="Transparent"
             x:Name="roundedEntry">
  <ContentView.Content>
        <Frame HasShadow="False"
               BackgroundColor="{Binding Source={x:Reference roundedEntry}, Path=EntryBackgroundColor}"
               CornerRadius="{Binding Source={x:Reference roundedEntry}, Path=CornerRadius}"
               Padding="15, 0, 15, 0">
            <Entry 
                FontSize="14"
               Text="{Binding Source={x:Reference roundedEntry}, Path=Text}"
               Placeholder="{Binding Source={x:Reference roundedEntry}, Path=Placeholder}"
               PlaceholderColor="LightGray">
                <Entry.Effects>
                    <effects:NoUnderlineEffect></effects:NoUnderlineEffect>
                </Entry.Effects>
            </Entry>
        </Frame>
    </ContentView.Content>
</ContentView>