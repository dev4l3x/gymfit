<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:routines="clr-namespace:FitnessTrack.Views.Routines"
             x:Class="FitnessTrack.Views.Routines.AddRoutinePage"
             Style="{StaticResource PageStyle}"
             BackgroundColor="White">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <routines:RoutineInfoView x:Name="PageOne"></routines:RoutineInfoView>

                <routines:AddExercisesView IsVisible="False"
                                          x:Name="PageTwo"></routines:AddExercisesView>

                <Button Text="CONTINUAR"
                        CornerRadius="10"
                        Style="{StaticResource ButtonStyle}"
                        Clicked="Button_Clicked"
                        x:Name="ContinueButton"></Button>

                <Button Text="CREAR"
                        CornerRadius="10"
                        Style="{StaticResource ButtonStyle}"
                        IsVisible="false"
                        x:Name="CreateButton"></Button>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>